{
  "name": "spong",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build --base='./'",
    "serve": "vite preview",
    "gh-pages": "git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "vite": "^2.5.4"
  },
  "dependencies": {
    "kaboom": "2000.0.0-beta.23"
  }
}
